# Onboarding: Начало работы

Пошаговая инструкция для новичка (Windows).

---

## 1. Получи доступ

Тебе должны дать:
- **Email** — для входа в Claude Code
- **Пароль** — от этого аккаунта

⚠️ **ВАЖНО:**
- Эту почту **НЕЛЬЗЯ использовать в личных целях**
- Claude в браузере (claude.ai) **использовать НЕЛЬЗЯ**
- Работай **ТОЛЬКО через терминал** (Claude Code)

Если не дали доступ — напиши тому, кто тебя пригласил.

---

## 2. Установи программы

### 2.1 Node.js

1. Открой https://nodejs.org/
2. Скачай **LTS версию** (зелёная кнопка)
3. Запусти установщик → жми "Next" везде
4. **Перезагрузи компьютер**

---

### 2.2 Git + Git Bash

**Git Bash** — терминал, в котором ты будешь работать.

1. Открой https://git-scm.com/download/win
2. Скачай и запусти установщик
3. При установке оставь всё по умолчанию
4. После установки найди **Git Bash** в меню Пуск

**Как открыть Git Bash:**
- Нажми Win → напиши "Git Bash" → Enter

---

### 2.3 GitHub аккаунт

1. Открой https://github.com
2. Нажми **"Sign up"**
3. Введи email, пароль, username
4. Подтверди email (придёт письмо)

---

### 2.4 GitHub CLI

1. Открой https://cli.github.com/
2. Нажми **"Download for Windows"**
3. Установи
4. **Закрой и открой Git Bash заново**

---

### 2.5 Авторизация в GitHub

Открой **Git Bash** и выполни:

```bash
gh auth login
```

Выбирай:
1. `GitHub.com`
2. `HTTPS`
3. `Yes` (authenticate Git)
4. `Login with a web browser`

В браузере:
1. Скопируй код из терминала
2. Вставь на странице GitHub
3. Нажми "Authorize"

**Проверь:**
```bash
gh auth status
```

Должно показать "Logged in to github.com"

---

### 2.6 Настрой Git

Замени на **свои данные**:

```bash
git config --global user.name "Твоё Имя"
git config --global user.email "твой@email.com"
```

---

### 2.7 Claude Code

```bash
npm install -g @anthropic-ai/claude-code
```

**Проверь:**
```bash
claude --version
```

Если ошибка — закрой Git Bash и открой заново.

---

## 3. Авторизация в Claude Code

```bash
claude
```

1. Откроется браузер
2. Войди через **почту которую тебе дали** (не свою!)
3. Подтверди доступ
4. Вернись в Git Bash

---

## 4. Склонируй проект

```bash
mkdir -p /c/projects
cd /c/projects
git clone https://github.com/michaelpautov/nha-trang.git
cd nha-trang
```

---

## 5. Проверь что всё работает

```bash
claude
```

Если открылся Claude Code — всё готово!

---

## ✅ Чеклист

Проверь перед тем как идти дальше:

- [ ] Node.js установлен (`node --version`)
- [ ] Git Bash работает
- [ ] GitHub аккаунт создан
- [ ] `gh auth status` показывает "Logged in"
- [ ] Git настроен (`git config --list`)
- [ ] Claude Code установлен (`claude --version`)
- [ ] Репозиторий склонирован
- [ ] `claude` запускается в папке проекта

---

## 6. Что дальше?

Переходи к задачам:

```bash
cd /c/projects/nha-trang
ls tasks/
```

Или скажи Claude:
> "Покажи какие есть задачи в tasks/"

Начни с папки которую тебе указали (например `tasks/visa/`).

---

## Проблемы?

### "claude" не найден
Закрой Git Bash → открой заново → попробуй ещё

### "npm" не найден
Node.js не установлен. Установи и **перезагрузи компьютер**.

### "gh" не найден
GitHub CLI не установлен. Установи и **перезапусти Git Bash**.

### Ошибка при npm install
```bash
npm install -g @anthropic-ai/claude-code --force
```

---

## Структура проекта

```
nha-trang/
├── ONBOARDING.md       # ← Ты здесь
├── CLAUDE.md           # Контекст для AI
├── tasks/              # Задачи
│   ├── visa/           # Визовый сайт
│   └── rent/           # Сайт аренды
└── docs/               # Документация
    └── rent/           # Проект аренды
```

---

## Полезные команды

```bash
pwd           # Где я?
ls            # Что тут лежит?
cd папка      # Перейти в папку
cd ..         # Назад
clear         # Очистить экран
```

---

Удачи! 🚀
